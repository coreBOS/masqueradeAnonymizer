potential:
  vtiger_potential:
    columns:
      potentialname:
        formatter: sentence
      amount:
        formatter: randomFloat
      forecast_amount:
        formatter: randomFloat
      email:
        formatter: email
